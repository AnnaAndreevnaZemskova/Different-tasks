/* Написать функцию, которая принимает два числа
и возращает результат их умножения */

function multiplications(num1, num2) {
  return num1 * num2;
}

// или

// const multiplications = (num1, num2) => num1 * num2;

console.log(multiplications(2, 44));
